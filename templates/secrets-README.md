# Publish secrets

Gitignored folder for publisher credentials.

| File | `keys.json` field |
|------|-------------------|
| Play Console service-account JSON | `play.serviceAccountJson` |
| App Store Connect AuthKey `.p8` | `apple.apiKeyP8` |

Replace placeholder values in `keys.json` before publishing. Paths in that file are relative to this folder.
